#!/usr/bin/env bash
set -euo pipefail

echo "SBP-064.1 rollback uses the timestamped pre-install app.js backup."
echo "No automatic rollback is performed because selecting the correct backup is an operator decision."
