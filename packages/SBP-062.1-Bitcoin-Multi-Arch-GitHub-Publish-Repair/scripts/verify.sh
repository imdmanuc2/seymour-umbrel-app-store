#!/usr/bin/env bash
set -euo pipefail
ROOT="${1:-$(cd "$(dirname "${BASH_SOURCE[0]}")/../../.." && pwd)}"
WF="$ROOT/.github/workflows/seymour-bitcoin-node-multiarch.yml"
echo "SBP-062.1 verify: Bitcoin multi-arch GitHub publish repair"
grep -q 'name: build-${{ matrix.arch }}' "$WF"
grep -q 'platform: linux/amd64' "$WF"
grep -q 'platform: linux/arm64' "$WF"
grep -q 'publish-manifest:' "$WF"
grep -q 'imagetools create' "$WF"
grep -q 'BITCOIN_VERSION}-amd64' "$WF"
grep -q 'BITCOIN_VERSION}-arm64' "$WF"
grep -q "grep -q 'linux/amd64'" "$WF"
grep -q "grep -q 'linux/arm64'" "$WF"
echo "SBP-062.1 split build matrix contract: PASS"
echo "SBP-062.1 architecture tag contract: PASS"
echo "SBP-062.1 manifest publish gate contract: PASS"
echo "SBP-062.1 final verification: PASS"
echo "No image was published and no live runtime was modified."
