# SBP-002 — BCH Provisioning Wizard

Adds Recommended and Advanced provisioning-plan workflows for:
- fresh sync
- copy existing node
- restore backup
- remote RPC

Run:

```bash
chmod +x scripts/*.sh
./scripts/doctor.sh
./scripts/install.sh
./scripts/verify.sh
```

Source-only package. It does not execute provisioning.
