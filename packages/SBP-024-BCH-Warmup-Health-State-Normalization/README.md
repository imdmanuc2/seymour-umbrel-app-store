# SBP-024 — BCH Warmup & Health State Normalization

Normalizes BCH Docker health during RPC warmup and wires Umbrel lifecycle post-operation state reconciliation into the real execute path. No live restart is executed automatically.
