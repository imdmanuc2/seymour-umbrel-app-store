#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(
  cd "$(dirname "${BASH_SOURCE[0]}")/.."
  pwd
)"

export PYTHONPATH="$REPO_ROOT/shared${PYTHONPATH:+:$PYTHONPATH}"

exec python3 \
  -m umbrel_runtime.cli \
  --app-store-root "$REPO_ROOT" \
  "$@"
