services:
  app_proxy:
    environment:
      APP_HOST: seymour-dashboard_web_1
      APP_PORT: 80

  web:
    image: nginx:alpine
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR}/data/html:/usr/share/nginx/html:ro
